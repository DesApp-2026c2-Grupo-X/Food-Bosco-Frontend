# URL del servidor de backend (GraphQL gateway)
VITE_API_URL=http://localhost:4000

# URL de la app admin global (el login redirige acá si el usuario es super_admin)
VITE_ADMIN_URL=http://localhost:5174

# URL de la app branch (el login redirige acá si el usuario es admin de sucursal)
VITE_BRANCH_URL=http://localhost:5175

# URL de la app rider
VITE_RIDER_URL=http://localhost:5176

# API key de Geoapify para el mapa estático de ruta del viaje
VITE_GEOAPIFY_API_KEY=

# Si es true, RequireAuth mockea la sesión como rider (no redirige al login).
VITE_MOCK_AUTH=false
