# ---------------------------------------------------------------------------
# Variables de entorno — app admin (admin global)
#
# [Requerida]  Sin ella la app no funciona.
# [Opcional]   Tiene valor por defecto en el código (packages/auth/appConfig.ts).
# ---------------------------------------------------------------------------

# [Requerida] URL del backend (GraphQL gateway). En dev el proxy de Vite cae a
# http://localhost:4000 si no se define; en build/deploy es obligatoria.
VITE_API_URL=http://localhost:4000

# [Requerida para mapas] API key de Geoapify para geocodificar la dirección de la
# sucursal y mostrar el mapa. Si está vacía, el mapa y el geocoding se desactivan.
VITE_GEOAPIFY_API_KEY=

# [Opcional] URL de la app admin global. Default: http://localhost:5174
VITE_ADMIN_URL=http://localhost:5174

# [Opcional] URL de la app branch (redirect del login si el rol es admin de
# sucursal). Default: http://localhost:5175
VITE_BRANCH_URL=http://localhost:5175

# [Opcional] URL de la app rider (redirect del login si el rol es repartidor).
# Default: http://localhost:5176
VITE_RIDER_URL=http://localhost:5176

# [Opcional] Si es true, RequireAuth mockea la sesión (no redirige al login).
# Default: false
VITE_MOCK_AUTH=false
